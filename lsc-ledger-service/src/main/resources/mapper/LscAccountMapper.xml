<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lianshengtong.ledger.mapper.LscAccountMapper">

    <!-- 初始化账户(若不存在则插入，存在则忽略) -->
    <insert id="insertIfNotExists">
        INSERT IGNORE INTO lsc_accounts (user_id, total_locked, total_available, version)
        VALUES (#{userId}, 0, 0, 1)
    </insert>

    <!-- 查询所有锁定余额 > 0 的账户(跨分片广播) -->
    <select id="selectAllLockedAccounts" resultType="com.lianshengtong.ledger.entity.LscAccount">
        SELECT user_id, total_locked, total_available, version, updated_at
        FROM lsc_accounts
        WHERE total_locked > 0
    </select>

    <!-- 查询所有有可用明细的用户ID(跨分片广播) -->
    <select id="selectUserIdsHavingAvailableDetails" resultType="java.lang.Long">
        SELECT DISTINCT user_id
        FROM available_lsc_details
        WHERE status = 1
    </select>

</mapper>
