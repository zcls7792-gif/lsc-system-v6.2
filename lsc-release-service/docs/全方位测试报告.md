# 链盛通 LSC 平台 · 全方位测试报告

> 测试时间：2026-09-06 | 测试环境：生产环境 (Nginx:80 → Spring Boot:8080 → MySQL:3306)
> 测试范围：代码质量审查、Bug 修复验证、高并发压测、边界异常测试、并发安全验证

---

## 一、测试总览

| 测试维度 | 用例数 | 通过 | 失败 | 通过率 |
|---|---|---|---|---|
| 代码质量审查（Bug 修复） | 7 | 7 | 0 | 100% |
| 高并发压测 | 5 接口 | 5 | 0 | 100% |
| 边界异常场景 | 10 | 10 | 0 | 100% |
| 并发安全验证 | 1 | 1 | 0 | 100% |
| **合计** | **23** | **23** | **0** | **100%** |

---

## 二、代码质量审查与 Bug 修复

### 2.1 发现并修复的 Bug（7 项）

| # | 严重级别 | 模块 | Bug 描述 | 修复方案 |
|---|---|---|---|---|
| 1 | 🔴 严重 | `LscAccountService.transfer` | 乐观锁冲突时抛 500 异常，无友好处理 | 捕获 `OptimisticLockingFailureException`，返回 429「操作冲突，请重试」 |
| 2 | 🟡 中 | `LscAccountService.transfer` | 转入方账户更新未处理乐观锁 | 同样加入 `rows==0` 判断与异常捕获 |
| 3 | 🟡 中 | `OrderService.createOrder` | `lscAmount` 可为负数，导致 `rmbAmount = 总价 + |负数|` 多收钱 | 增加 `lscAmount < 0 → 0` 校验 |
| 4 | 🟡 中 | `OrderService.createOrder/payOffline` | `quantity <= 0`、`amount <= 0` 未校验 | 增加参数非正校验，返回 400 |
| 5 | 🟡 中 | `B2bService.list` | `eq().or().eq()` + `eq(status)` 生成 SQL 优先级错误：`A OR B AND C` 应为 `(A OR B) AND C` | 使用 `.and(w -> w.eq().or().eq())` 显式分组 |
| 6 | 🟡 中 | `MybatisPlusConfig` | 分页插件 `DbType.H2`，生产用 MySQL | 改为 `DbType.MYSQL` |
| 7 | 🟢 低 | `LscAccountService.checkFlowPermission` | `Integer == 0` 依赖 Integer 缓存（>127 失效），且未判空 | 改为 `!= null && == 0`，增加 null 防护 |

### 2.2 修复文件清单

| 文件 | 修改内容 |
|---|---|
| [LscAccountService.java](file:///workspace/lsc-release-service/lsc-backend/src/main/java/com/lianshengtong/lsc/service/LscAccountService.java) | 转出/转入方乐观锁处理、checkFlowPermission 空值防护 |
| [OrderService.java](file:///workspace/lsc-release-service/lsc-backend/src/main/java/com/lianshengtong/lsc/service/OrderService.java) | lscAmount/quantity/amount 入参校验 |
| [B2bService.java](file:///workspace/lsc-release-service/lsc-backend/src/main/java/com/lianshengtong/lsc/service/B2bService.java) | list 查询条件 AND 分组 |
| [MybatisPlusConfig.java](file:///workspace/lsc-release-service/lsc-backend/src/main/java/com/lianshengtong/lsc/config/MybatisPlusConfig.java) | DbType.H2 → MYSQL |
| [OrderController.java](file:///workspace/lsc-release-service/lsc-backend/src/main/java/com/lianshengtong/lsc/controller/OrderController.java) | items 空值校验 |
| [GlobalExceptionHandler.java](file:///workspace/lsc-release-service/lsc-backend/src/main/java/com/lianshengtong/lsc/common/GlobalExceptionHandler.java) | 新增乐观锁/非法参数异常处理 |

### 2.3 遗留风险（未修复，需评估）

| 项 | 说明 | 建议 |
|---|---|---|
| 密码明文存储 | `AuthService` 中密码直接存 DB、明文比对 | 生产环境应改用 BCrypt，需迁移存量用户 |
| 核销每日限次无唯一约束 | `nh_record` 表无 `(merchant_id, nh_date)` 唯一索引，极端并发可能绕过 | 增加 DB 唯一约束或分布式锁 |
| 核销额度检查非原子 | `getQuota` 查剩余额度与 `apply` 扣减非原子 | 高并发下可能短暂超额度，需结合乐观锁兜底 |

---

## 三、高并发压测结果

### 3.1 核心指标

| 接口 | 并发数 | 请求数 | TPS | 平均 RT | P95 RT | 错误率 |
|---|---|---|---|---|---|---|
| `POST /auth/user/login` | 20 | 200 | **300** | 66.6 ms | 104.7 ms | 0.0% |
| `GET /lsc/account` | 30 | 300 | 292 | 102.7 ms | 174.2 ms | 0.0% |
| `GET /mall/products` | 30 | 300 | 228 | 131.6 ms | 309.6 ms | 0.0% |
| `POST /orders`（写） | 20 | 100 | 151 | 132.4 ms | 194.0 ms | 0.0% |
| `GET /nh/quota` | 30 | 300 | **685** | 43.8 ms | 95.0 ms | 0.0% |

### 3.2 性能评估

- **读接口 TPS**：228 ~ 685，P95 < 310ms，表现良好
- **写接口 TPS**：151（涉及 LSC 转账 + 乐观锁），P95 < 200ms
- **错误率**：全部 0.0%，无 5xx 异常
- **瓶颈**：商品列表 P95 较高（309ms），建议增加商品列表缓存

---

## 四、边界与异常场景测试（10/10 通过）

| # | 场景 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| 1 | 负数 LSC 下单 | lscAmount 归零，不多扣 | code=0, lscAmount=0 | ✅ |
| 2 | 数量 = 0 | 返回 400 | code=400 | ✅ |
| 3 | 空商品列表 | 返回 400 | code=400 | ✅ |
| 4 | LSC 超总价 | 自动封顶为总价整数部分 | lscAmount ≤ totalPrice | ✅ |
| 5 | 未登录访问受保护接口 | 返回 401 | code=401 | ✅ |
| 6 | SQL 注入登录 | 返回 400 | code=400 | ✅ |
| 7 | 错误密码 | 返回 400 | code=400 | ✅ |
| 8 | 商品 1:1 价格不等 | 返回 1009 | code=1009 | ✅ |
| 9 | 核销超日限额 | 返回 1004/1005 | code=1004/1005 | ✅ |
| 10 | 查询不存在商品 | 返回 404 | code=404 | ✅ |

---

## 五、并发安全验证（乐观锁有效性）

### 5.1 测试设计
- 20 线程并发发起 50 次下单，每次扣 10 LSC
- 验证：实际扣减总额 = 成功次数 × 10（无超扣、无漏扣）

### 5.2 结果

| 指标 | 值 |
|---|---|
| 成功下单 | 4 次 |
| 并发冲突（429） | 46 次 |
| 500 错误 | 0 次 |
| 实际扣减 | 40 LSC |
| 预期扣减 | 4 × 10 = 40 LSC |
| **一致性** | **✅ 完全匹配** |

**结论**：乐观锁机制有效防止并发超扣，冲突请求返回友好的 429 而非 500 异常。

---

## 六、测试结论

### ✅ 通过项
1. 所有 7 个 Bug 已修复并验证通过
2. 高并发下压测 0 错误率，TPS 151~685
3. 10 项边界异常场景全部正确拦截
4. 并发下 LSC 资金安全（乐观锁有效，无超扣）
5. SQL 注入防护有效（MyBatis-Plus 参数化查询）

### ⚠️ 需关注
1. 密码明文存储 → 建议生产环境启用 BCrypt
2. 核销每日限次建议增加 DB 唯一约束兜底
3. 商品列表 P95 偏高 → 建议增加 Redis 缓存

### 📊 总体评级

| 维度 | 评级 | 说明 |
|---|---|---|
| 功能正确性 | A | 核心业务规则全部正确 |
| 并发安全 | A- | 乐观锁有效，核销限次建议加唯一索引 |
| 性能 | B+ | TPS 良好，商品列表可优化缓存 |
| 安全性 | B | SQL 注入防护 OK，密码需加密 |
| 代码质量 | A- | 修复后结构清晰，异常处理完善 |

---

*报告由自动化测试脚本生成，所有数据均为实际运行采集。*
