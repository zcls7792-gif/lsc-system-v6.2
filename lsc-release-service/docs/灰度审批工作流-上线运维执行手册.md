# 灰度发布审批工作流 — 上线运维执行手册 (Phase M Go-Live Runbook)

> 版本：v6.2.0-AI · 执行日期：2026-09-04 · 作者：LSC Platform
> 适用服务：`lsc-release-service`（审批引擎）+ `lsc-gateway`（执行后端，已在 v6.2 基线）
> 风险等级：**Medium**（DDL 新增表无数据迁移；无老代码删除；回滚点清晰）

---

## 🔴 上线前准备（T-1 日，Change Freeze 前）

### 1. 交付物清单

| # | 交付物 | 当前位置 | 目标位置（生产） |
| - | ------ | -------- | ---------------- |
| 1 | **可部署 Jar**：`lsc-release-service.jar` (128MB) | `/workspace/lsc-release-service/target/` | K8s / VM 发布机（或走镜像流水线） |
| 2 | **Flyway DDL**：`V6.2.0__gray_approval_flow.sql` | `lsc-release-service/src/main/resources/db/migration/` | `lsc_release` 库通过 Flyway 自动执行，**或** DBA 手动执行 |
| 3 | **Nacos 配置**：`gray.approval.*` + `alert.*` | 见 §2 | Nacos `LSC_GROUP` |
| 4 | **XXL-JOB 任务登记** × 3 | 见 §3 | XXL-JOB Admin 控制台 |
| 5 | **环境变量** × 15 | 见 §4 | Pod / systemd / 启动脚本 |
| 6 | **镜像（若容器化）** | 用交付 Jar 构建镜像 | Harbor / Registry |

### 2. Nacos 配置变更（LSC_GROUP）

在 Nacos Console → `lsc-gateway-routes.yaml` / `lsc-release-service` 应用配置：

```yaml
# ====== [NEW] 灰度审批工作流参数 ======
gray:
  approval:
    default-required-approvals: 2          # 创建审批单默认审批人数（关键动作建议改为3）
    approver-role: ROLE_RELEASE_ADMIN      # 审批人池角色，需与 lsc-user-service 角色中心对齐
    execute-retry-max: 3                   # 自愈任务允许的 EXECUTE_FAILED 自动重试次数
    audit-chain-enabled: false             # 链上存证（金融合规需求时 true）
    stale-executing-seconds: 120           # EXECUTING 超过 2 分钟视为"卡住"，触发回查修复
    self-heal-retry-interval-seconds: 600  # 两次自动重试之间 ≥10 分钟，避免打爆网关
    approver-idle-remind-hours: 24         # 审批人 24h 未处理提醒（下一版提醒任务）

# ====== [NEW] 告警通道（生产切换飞书）======
alert:
  channel: feishu                                    # 默认值 logging，改为 feishu 推送到群
  feishu:
    webhook-url: ${ALERT_FEISHU_WEBHOOK}             # 从 Nacos Secret / 环境变量注入，禁止明文！

# ====== [EXISTING] 保留原 release 配置 ======
release:
  alert-receivers: ${ALERT_RECEIVERS:admin-super-001,admin-super-002}  # 告警接收人 @user_id 列表（英文逗号分隔）
```

⚠️ **Nacos 变更操作顺序**：先 **保存→发布** 配置，再滚动重启服务（服务端 bootstrap 会热拉）；
⚠️ `ALERT_FEISHU_WEBHOOK` 如果走环境变量注入，**不要**在 YAML 明文写死；Nacos Secret 加密配置最佳实践参考 `lsc-common-infra.yaml`。

### 3. XXL-JOB 任务登记（3 个 Handler）

登录 XXL-JOB Admin → 执行器管理 → 选中 `lsc-release-service` → 任务管理 → 新增 3 条：

| JobHandler                  | 调度类型  | Cron               | 运行模式 | 路由策略     | 阻塞处理策略 | 任务描述（建议）                                           | 优先级 |
| --------------------------- | --------- | ------------------ | -------- | ------------ | ------------ | ---------------------------------------------------------- | ------ |
| `grayAuditJob`              | Cron      | `0 0/10 * * * ?`   | Bean     | FIRST        | 单机串行     | Phase P：网关 rollout/status 巡检 + SLO 连续失败/僵尸策略 告警 | P1     |
| `grayApprovalSelfHealJob`   | Cron      | `0 0/5 * * * ?`    | Bean     | FIRST        | 单机串行     | Phase M：EXECUTING 卡死修复 + EXECUTE_FAILED 自动重试       | **P0** |
| `grayStalePolicyEnforceJob` | Cron      | `0 30 3 * * ?`     | Bean     | FIRST        | 单机串行     | Phase P：日度僵尸策略扫描（当前 skeleton，后续版本激活） | P2     |

> **为什么 `FIRST + 单机串行`**：自愈任务会写 DB、调用审批 Service；多实例并发执行会导致同一个 EXECUTE_FAILED flow 被多次 retry。
>
> **为什么阻塞处理=单机串行**：自愈任务最长可能执行 3 分钟（回查 gateway policyStats 有网络 IO），叠加默认 5 min 间隔，避免同一 executor 内新调度线程与上一轮重叠。

### 4. 环境变量注入清单（15 项）

按字母顺序整理（方便 DevOps 批量核对）：

| 变量名                                       | 类型   | 生产建议值                               | 说明 |
| -------------------------------------------- | ------ | ---------------------------------------- | ---- |
| `ALERT_CHANNEL`                              | string | `feishu`                                 | 启用飞书告警通道 |
| `ALERT_FEISHU_WEBHOOK`                       | string | `(加密)`                                 | 飞书群机器人完整 webhook（含 sign secret） |
| `ALERT_RECEIVERS`                            | string | `ou_xxx,ou_yyy`                          | P0/P1 告警接收人（飞书 user_id / 邮箱） |
| `GRAY_APPROVAL_AUDIT_CHAIN`                  | bool   | `false`                                  | 合规阶段后打开 |
| `GRAY_APPROVAL_DEFAULT_REQUIRED`             | int    | `2` 或 `3`                               | 默认审批人数 |
| `GRAY_APPROVAL_IDLE_REMIND_H`                | int    | `24`                                     | 空闲审批人提醒小时数 |
| `GRAY_APPROVAL_RETRY_INTERVAL_SEC`           | int    | `600`                                    | 自愈重试间隔秒 |
| `GRAY_APPROVAL_RETRY_MAX`                    | int    | `3`                                      | 自愈最大重试次数 |
| `GRAY_APPROVAL_ROLE`                         | string | `ROLE_RELEASE_ADMIN`                     | 审批人角色 |
| `GRAY_APPROVAL_STALE_EXECUTING_SEC`          | int    | `120`                                    | EXECUTING 判定"卡住"秒数 |
| `MYSQL_HOST` / `MYSQL_PORT` / `MYSQL_USER` / `MYSQL_PWD` | 原基线 | — | `lsc_release` 库连接 |
| `NACOS_ADDR` / `NACOS_NAMESPACE`             | 原基线 | — | Nacos 地址 |
| `REDIS_HOST` / `REDIS_PORT` / `REDIS_PWD`    | 原基线 | — | Redisson 分布式锁 |
| `SEATA_ADDR`                                 | 原基线 | — | Seata AT 模式 |
| `XXL_JOB_ADMIN` / `XXL_JOB_TOKEN`            | 原基线 | — | XXL-JOB 调度 |

---

## 🟠 上线当天执行步骤（T-Day，建议低峰 02:00-04:00）

### Step 0：变更会议 + 回滚点确认（10min，必须）

✅ 参会：DBA（如手动 DDL）、DevOps（发布）、研发（Owner）、SRE（值守）
✅ 确认回滚版本：`v6.2-AI-pre-gray-approval`（上线前的镜像 tag）
✅ 确认公告窗口：已通知用户"灰度审批模块"上线，可能出现首次审批需要重新提交的情况

### Step 1：执行 DDL（Flyway 自动 或 DBA 手动）（10min）

**场景 A：使用 Flyway**
```bash
# release-service 启动参数（spring.flyway.enabled=true，默认基线已启用）
java -jar lsc-release-service.jar \
  --spring.flyway.enabled=true \
  --spring.flyway.locations=classpath:db/migration \
  --spring.flyway.baseline-on-migrate=true
# 启动后检查：
# SHOW DATABASE CHANGELOG WHERE FILENAME LIKE '%gray_approval%';
```

**场景 B：DBA 手动执行（不使用 Flyway）**
```sql
-- =============== 登录 lsc_release 库 ===============
-- 文件：lsc-release-service/src/main/resources/db/migration/V6.2.0__gray_approval_flow.sql

SOURCE ./V6.2.0__gray_approval_flow.sql;

-- =============== 执行后立即 DDL 校验 ===============
SELECT COUNT(*) AS tab_count FROM information_schema.TABLES
WHERE TABLE_SCHEMA='lsc_release' AND TABLE_NAME IN (
  'gray_approval_flow','gray_approval_node','gray_approval_audit'
);
-- ✅ 预期：tab_count = 3

SELECT INDEX_NAME, COLUMN_NAME FROM information_schema.STATISTICS
WHERE TABLE_SCHEMA='lsc_release' AND TABLE_NAME='gray_approval_flow' ORDER BY SEQ_IN_INDEX;
-- ✅ 应包含 PRIMARY(id), uk_flow_no, idx_policy_status, idx_applicant, idx_status_created
```

### Step 2：推送 Nacos 配置（5min）

参考 §2，在 Nacos Console 发布 `lsc-release-service` 的 2 段新配置。
**✅ 发布后校验**：Nacos 监听监听者列表 → `lsc-release-service` 旧实例（`n-1` 个）应全部显示"已订阅最新配置"。

### Step 3：滚动发布 lsc-release-service（20min）

#### K8s（推荐）
```bash
# helm upgrade 或 kubectl set image
kubectl set image deployment/lsc-release-service \
  lsc-release-service=registry.lsc.local/lsc-release-service:v6.2.0-gray-approval-m1 \
  -n lsc-production

# 金丝雀步：20% 流量（1 个 Pod）→ 观察 10 分钟 → 全量
kubectl rollout status deployment/lsc-release-service -n lsc-production
# ✅ 每一步观察：kubectl logs -f {新Pod} | grep -E "ERROR|GrayApproval"
```

#### VM（systemd）
```bash
sudo systemctl stop lsc-release-service
sudo cp /opt/lsc-release-service.jar{,.bak.v6.1}
sudo cp ./lsc-release-service.jar /opt/
sudo systemctl start lsc-release-service
sudo journalctl -u lsc-release-service -f | grep -E "Started|ERROR|GrayApproval"
# ✅ 观察：无 ERROR 日志，进程 10min 不重启
```

### Step 4：XXL-JOB 任务登记与启动作废（5min）
参考 §3，在 Console 新增 3 个任务。
**✅ 新增后点"执行一次"**：
- grayAuditJob：查看执行日志 → `grayAuditJob done; policies scanned=` 数字正常（不是 0 且无异常栈）
- grayApprovalSelfHealJob：查看执行日志 → `grayApprovalSelfHeal done: stuckExecutingFixed=0, autoRetried=0`（首次都是 0 是正常的）
- grayStalePolicyEnforceJob：允许显示 "PLACEHOLDER: no-op"

### Step 5：冒烟测试（15min，立即执行，见 §5）
对 5 条接口用 curl 逐一跑一遍，确保端点可到达、JWT 鉴权通过、数据落库正确。

### Step 6：观察窗口（30min 值守）
监控项（Grafana Dashboard `gray-rollout`）：
- ✅ JVM 堆内存 < 75%（新增 Micrometer Counter/Gauge 有小对象开销）
- ✅ `gray_approval_lock_contention_total{success="false"}` = 0（锁冲突成功应全为 true，false=拿不到锁 → 排查 Redis）
- ✅ XXL-JOB 两次调度都正常（间隔 5/10min）
- ✅ `lsc-release-service` Pod 重启次数 = 0

---

## 🟡 冒烟测试用例（5 条 curl + 3 个场景流程）

> 以下命令假设您通过 `lsc-gateway` 路由（推荐生产走网关统一鉴权）。
> 环境变量 `LSC_ADMIN_TOKEN` = 登录后 JWT；`BASE` = `https://admin.lsc.prod.lianshengtong.com`（管理台入口域名）。
> 所有路径前缀与 [GrayApprovalController](file:///workspace/lsc-release-service/src/main/java/com/lianshengtong/release/controller/GrayApprovalController.java) 一致。

```bash
export BASE="https://admin.lsc.prod.lianshengtong.com"
export TOKEN="Bearer <your_admin_jwt>"
export H="Authorization:${TOKEN}"
export CT="Content-Type:application/json"
```

---

### 冒烟 1：创建审批单（GRADUATE，默认 2 人审批）

```bash
curl -sS -X POST "$BASE/api/release/gray/approvals" \
  -H "$H" -H "$CT" -d '{
  "flowType": "GRADUATE",
  "policyId": "smoke-test-policy-20260904",
  "applicant": "ops_smoke@lianshengtong.com",
  "title": "[SMOKE] 订单服务v2毕业",
  "applyReason": "上线前冒烟测试：错误率达标",
  "requiredApprovals": 2,
  "approvers": ["approver_a@lianshengtong.com","approver_b@lianshengtong.com"]
}' | python3 -m json.tool
# ✅ 校验：
#   code == 0
#   data.status == "PENDING_APPROVAL"
#   data.flowNo 以 "GA20260904" 开头
#   data.requiredApprovals == 2  &&  data.approvedCount == 0
# 👉 记录返回 data.id 为 $FLOW_ID（后续用）
export FLOW_ID="<从data.id取>"
```

---

### 冒烟 2：第 1 人审批通过（approver_a — 1/2 通过，状态仍 PENDING）

```bash
curl -sS -X PUT "$BASE/api/release/gray/approvals/action/approve" \
  -H "$H" -H "$CT" -d "{
  \"flowId\": $FLOW_ID,
  \"approver\": \"approver_a@lianshengtong.com\",
  \"approved\": true,
  \"comment\": \"[SMOKE] 错误率 0.05% < 0.1% 阈值，同意通过\"
}" | python3 -m json.tool
# ✅ 校验：data.approvedCount == 1  &&  data.status == "PENDING_APPROVAL"
# ❌ 常见失败：approver 字段与 JWT 用户不一致 → 检查 JWT 是否对应 approver_a 账号
```

---

### 冒烟 3：第 2 人审批（approver_b — 2/2 通过 → 触发自动执行网关 graduate）

> 💡 由于 policyId "smoke-test-policy-20260904" **在网关可能不存在**，会走到 fallbackFactory，返回 code=503 fallback。
> 此时审批单状态应为 `EXECUTE_FAILED`（这是**预期行为**，证明 fallback + 状态机正确）。
> 如果网关中真实配置了该策略 → 则直达 `SUCCEEDED`。

```bash
curl -sS -X PUT "$BASE/api/release/gray/approvals/action/approve" \
  -H "$H" -H "$CT" -d "{
  \"flowId\": $FLOW_ID,
  \"approver\": \"approver_b@lianshengtong.com\",
  \"approved\": true,
  \"comment\": \"[SMOKE] 观察期 7 天无事故，同意上线\"
}" | python3 -m json.tool
# ✅ 两种可能都算 PASS：
#    A. 网关有策略 → status == "SUCCEEDED" + executeResponse 包含 graduated:true
#    B. 网关无策略/不可达 → status == "EXECUTE_FAILED" + executeResponse 包含 "fallback"
#       (B 是 fallbackFactory 在生效，证明降级正确；用冒烟 4 再手工重试即可)
```

---

### 冒烟 4：重试 EXECUTE_FAILED（运维手动按钮场景）

```bash
curl -sS -X PUT "$BASE/api/release/gray/approvals/action/retry-execute" \
  -H "$H" -H "$CT" -d "{
  \"flowId\": $FLOW_ID,
  \"operator\": \"ops_smoke@lianshengtong.com\"
}" | python3 -m json.tool
# ✅ 校验：status 根据网关可达性在 SUCCEEDED 或 EXECUTE_FAILED 之间切换，且每次都写入新的 audit
```

---

### 冒烟 5：查询详情（flow + 2 nodes + 全量审计流水）

```bash
curl -sS "$BASE/api/release/gray/approvals/$FLOW_ID" -H "$H" | python3 -m json.tool
# ✅ 校验：
#   data.flow.id == $FLOW_ID
#   len(data.nodes) == 2  &&  data.nodes[0].approver == "approver_a@..."  &&  data.nodes[0].nodeStatus == "APPROVED"
#   data.audits.length ≥ 5 (FLOW_CREATED, FLOW_SUBMITTED, NODE_APPROVED×1, FLOW_APPROVED, FLOW_EXECUTING, FLOW_SUCCEEDED/EXECUTE_FAILED)
#   audits 每条都有 action + operator + created_at 非空（append-only 时间线）
```

---

### 补充冒烟：2 个完整场景

#### 场景 X：WEIGHT_CHANGE（放量 10 → 50）

```bash
# Step X-1：创建（payload 必须带 targetWeight）
RESP=$(curl -sS -X POST "$BASE/api/release/gray/approvals" -H "$H" -H "$CT" -d '{
  "flowType":"WEIGHT_CHANGE","policyId":"order-service-xxx","applicant":"ops_smoke@xxx",
  "title":"放量 10%→50%","applyReason":"小流量稳定","requiredApprovals":1,
  "payload":{"targetWeight":50}
}')
FID=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['data']['id'])")

# Step X-2：单人审批（1/1）→ changeWeight 网关执行
curl -sS -X PUT "$BASE/api/release/gray/approvals/action/approve" -H "$H" -H "$CT" -d "{\"flowId\":$FID,\"approver\":\"ops_smoke@xxx\",\"approved\":true}" | python3 -c "import sys,json;d=json.load(sys.stdin)['data'];print(d['status'],' | ', d.get('executeCostMs'), 'ms')"
# ✅ 输出：SUCCEEDED  |  237 ms
```

#### 场景 Y：ROLLBACK 审批被拒 → REJECTED 终态，无法再 cancel / retry

```bash
# Step Y-1：创建 rollback 审批
RESP=$(curl -sS -X POST "$BASE/api/release/gray/approvals" -H "$H" -H "$CT" -d '{
  "flowType":"ROLLBACK","policyId":"order-service-xxx","applicant":"ops_smoke@xxx",
  "applyReason":"错误率 2.3% > 1% 阈值","requiredApprovals":2,
  "payload":{"reason":"错误率超阈值"}
}')
FID=$(echo "$RESP" | python3 -c "import sys,json;print(json.load(sys.stdin)['data']['id'])")

# Step Y-2：审批人 A 拒绝
curl -sS -X PUT "$BASE/api/release/gray/approvals/action/approve" -H "$H" -H "$CT" -d "{\"flowId\":$FID,\"approver\":\"risk_officer@xxx\",\"approved\":false,\"comment\":\"先抓包分析\"}" \
  | python3 -c "import sys,json;print('status=',json.load(sys.stdin)['data']['status'])"
# ✅ status= REJECTED

# Step Y-3：第二个人再想通过 → 应该被状态机拒绝
curl -sS -X PUT "$BASE/api/release/gray/approvals/action/approve" -H "$H" -H "$CT" -d "{\"flowId\":$FID,\"approver\":\"eng_manager@xxx\",\"approved\":true}" \
  | python3 -c "import sys,json;d=json.load(sys.stdin);print('code=',d.get('code'),'msg=',d.get('message'))"
# ✅ code= 409  msg= flow not pending: REJECTED（或 409 前缀）
```

---

## 🔵 回滚预案（Rollback Plan — 执行 Step 1 前必须演练一次）

### 分类 A：代码回滚（最常见，10min）

**触发条件**：冒烟测试出现 5xx、Pod 频繁重启、XXL-JOB 连续失败。

```bash
# ===== K8s 回滚 =====
kubectl rollout undo deployment/lsc-release-service -n lsc-production
kubectl rollout status deployment/lsc-release-service -n lsc-production
# ✅ 验证：kubectl get pods -n lsc-production -o wide | grep release-service → 全为老镜像 tag

# ===== VM/systemd 回滚 =====
sudo systemctl stop lsc-release-service
sudo cp /opt/lsc-release-service.jar.bak.v6.1 /opt/lsc-release-service.jar
sudo systemctl start lsc-release-service
```

### 分类 B：配置回滚（Nacos，5min）

**触发条件**：飞书告警刷屏 / SelfHeal 重复推送 P0（阈值过严）。

Nacos Console → `lsc-release-service` 配置 → **历史版本** → 回滚到"上线前版本（YYYYMMDD-HHMM）"。
```yaml
# 紧急临时降级（立刻屏蔽审批路径，不会导致 404，但所有审批行为不再落新表）：
alert.channel: logging          # 静默飞书告警
gray.approval.execute-retry-max: 0  # 彻底停止自愈自动重试
```
> 备注：Nacos 配置热生效，**无需重启 Pod**。

### 分类 C：DDL 回滚（Flyway undo / 手工清理，15min）

**触发条件**：出现锁等待、数据库连接池被 `gray_approval_audit` 批量 insert 打爆（不常见，但预有准备）。

```sql
-- ============== 登录 lsc_release 库 ==============
-- 新表都是 append-only，没有任何外键到老业务表；可以直接 DROP 或 RENAME 保留 7 天

-- 方案 1（保留 7 天数据便于排查，推荐）：
RENAME TABLE gray_approval_flow   TO gray_approval_flow_rollback_20260904;
RENAME TABLE gray_approval_node   TO gray_approval_node_rollback_20260904;
RENAME TABLE gray_approval_audit  TO gray_approval_audit_rollback_20260904;

-- 方案 2（彻底清除）：
DROP TABLE IF EXISTS gray_approval_audit;
DROP TABLE IF EXISTS gray_approval_node;
DROP TABLE IF EXISTS gray_approval_flow;
```

> 若用 Flyway：`flyway undo`（需企业版），或手动 `DELETE FROM flyway_schema_history WHERE version='6.2.0';` 后再 DROP。

### 分类 D：XXL-JOB 任务停用（立即生效，2min）

XXL-JOB Admin → 任务管理 → 搜索 `gray*` → 批量 **停用**（或点"停止"）。
Self-heal 任务造成异常告警风暴时，**优先先停任务**，再排查原因。

---

## 🟣 上线后观察 & 指标预期（Day 0 — Day 3）

| 观察窗口   | 指标                                                       | 正常范围                | 异常动作                                       |
| ---------- | ---------------------------------------------------------- | ----------------------- | ---------------------------------------------- |
| 上线后 1h  | SelfHeal stuckExecutingFixed / autoRetried                 | = 0 / 小 < 3            | >10 → 网关 policyStats 接口连通性排查           |
| Day 1      | `gray_approval_flow_created_total`（按 flowType）          | 预计 5-30，符合组织节奏 | 0 → 管理台菜单/权限没开，或前端未对接           |
| Day 1-3    | `gray_approval_node_decision_total{decision=REJECTED}`      | ≤ 20% 的审批单被拒     | >50% → 可能 SLO 门禁门槛过高或审批人培训不足    |
| Day 1-3    | `gray_approval_execute_latency_ms p99`                     | graduate < 1000ms       | >5s → 网关 graduate 路径性能排查                |
| Day 1-7    | `gray_approval_lock_contention_total{success=false}`        | =0（偶发 1 可忽略）     | 持续 >5 → Redis/网络异常或双实例分布式锁未生效   |
| Day 1-30   | Audit 无缺口（每条 flow 的 audit 数 ≥ 4 × 节点数）          | 100% 完整               | 缺任何一条 → 事务一致性检查 / DB 断连排查       |

---

## 🟤 常见报错与排查速查表（Runbook 附录）

| 错误码 | 典型消息                                               | 根因排查路径                                                                 |
| ------ | ------------------------------------------------------ | ---------------------------------------------------------------------------- |
| 409    | "flow not pending: REJECTED"                           | 审批人再次点击了已被拒绝的审批单；前端应按钮置灰                             |
| 409    | "no waiting node"                                      | 所有审批节点已处理完；或 approver != 匹配审批池 → 查审批人角色               |
| 409    | "cannot acquire distributed lock for flow 1001"        | 10s 内前一个审批没走完 → 排查 DB 慢查；或 2 人同时点同一个审批（正常限流）    |
| 503    | "GrayGatewayClient.graduate fallback: ConnectException" | lsc-gateway 实例下线 / Nacos 健康检查没发现 → 看 lsc-gateway Pod             |
| 500    | "LAUNCH type apply via policy upsert API directly"     | 前端误将"首次创建灰度"走审批单；应直接调 gateway /policies upsert            |
| 0      | SUCCEEDED 但 policy 实际没生效（"假成功"）              | **极其严重**；fallbackFactory 是否被替换成了静默 true？查 executeResponse     |

---

*运行手册结束。如有疑问联系：LSC Platform Team（lsc-platform@lianshengtong.com）*
